# Copy to: /etc/quoteflow/quoteflow.env
# Then: sudo systemctl daemon-reload && sudo systemctl restart quoteflow

NODE_ENV=production
PORT=3000
USE_REAL_DB=true
AUTO_MIGRATE=true

# Comma-separated allowlist for CORS
CORS_ORIGINS=https://wetalk.to,https://www.wetalk.to

# Public base URL for SEO routes (sitemap, canonical)
PUBLIC_BASE_URL=https://wetalk.to
SITE_NAME=WeTalkTo

# Required in production
JWT_SECRET=change-me-to-a-long-random-string

# Required when USE_REAL_DB=true
DATABASE_URL=postgresql://quoteflow_user:secure_password@localhost:5432/quoteflow

# Optional email (enables real email sending if set)
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=...
# EMAIL_PASS=...
# EMAIL_FROM="WeTalkTo <noreply@wetalk.to>"
